/**
 * @format
 */

/*
import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';

AppRegistry.registerCompindexonent(appName, () => App);
*/

// I couldn't find a way to make react-native use app/index.js directly
import './app/index.js';
